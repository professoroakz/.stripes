/**
 * @professoroakz/stripes
 * A comprehensive repository management system for GitHub codebases
 * with integrated EMS (Everything Running Seamlessly) functionality
 */

'use strict';

const { name, version, description } = require('./package.json');

module.exports = {
  name,
  version,
  description
};
